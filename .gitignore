# Dependencies
node_modules/

# Production & Build outputs
build/
dist/
coverage/
.firebase/

# OS and system files
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment and Secrets
.env
.env.*
!.env.example

# Service Accounts, Private Keys and Credentials
*.pem
*.key
*serviceAccount*.json
*service-account*.json
firebase-adminsdk*.json
credentials.json

# Supabase CLI (estado local, nunca versionado)
supabase/.branches/
supabase/.temp/

# Estado temporário do smoke test remoto (scripts/smoke-test-remote.ts)
.smoke-test-remote-state.json

# Relatório gerado por scripts/load-pilot-cardiologia.ts (saída de execução local, não é fonte)
load-pilot-cardiologia.report.txt

# Relatório gerado por scripts/load-compendios.ts (saída de execução local, não é fonte)
load-compendios.*.report.txt

# Relatório gerado por scripts/load-questoes.ts (saída de execução local, não é fonte)
load-questoes.*.report.txt

